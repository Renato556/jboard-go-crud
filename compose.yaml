services:
  crud-server:
    build:
      context: .
      target: final
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - app_net
    healthcheck:
      test: [ "CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  app_net:
    name: crud-network
